import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#5482A2',
        },
        secondary: {
            main: '#F89923',
        },
        background: {
            default: 'rgb(84,130,162, 0.2)',            
        }
    },
});