import { Box, Dialog, DialogTitle } from "@mui/material";

function StudentDialog({ mode }) {
  <Box>
    <Dialog>
      <DialogTitle></DialogTitle>
    </Dialog>
  </Box>;
}

export default StudentDialog;